{
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd client && npm start",
    "start:both": "cd backend && npm install && cd ../client && npm install && concurrently \"npm start\" \"npm start\" --prefix client"  }
}
